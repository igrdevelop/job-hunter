# Phase 3 — to_send removal checklist

Generated by: `grep -rn "to_send" --include="*.py" hunter/ tests/ tools/ apply_agent.py`

**15 files** require changes. Check off each as done.

---

## Files to DELETE

- [ ] `hunter/to_send.py`
- [ ] `tests/test_to_send_sync.py`
- [ ] `tools/check_expired_to_send.py`

## Files to RENAME

- [ ] `hunter/expired_to_send_check.py` → `hunter/expired_marker.py`
  Drop to_send logic; work directly against tracker.xlsx / cache

## hunter/ core

- [ ] `hunter/config.py` — remove `TO_SEND_PATH`
- [ ] `hunter/tracker.py` — remove `iter_rows_for_to_send()`, `apply_sent_updates()`
- [ ] `hunter/tracker_backup.py` — remove `_copy("to_send", ...)` and prune call
- [ ] `hunter/main.py` — remove lines 100-108 (`to_send.sync_and_rebuild` at hunt start)
- [ ] `hunter/services/tracker_service.py` — remove `to_send.sync_and_rebuild()` after apply
- [ ] `hunter/telegram_bot.py`
  - `/unsent` → use `tracker_cache.cache` directly (no `iter_rows_for_to_send`)
  - `/sync_sent` → no-op placeholder ("будет реализовано в Phase 5")
  - `/check_expired` → use new `expired_marker.run_check()` (works on tracker.xlsx directly)
  - `/apply_expired` → REMOVE (merged into `/check_expired`)
  - `_scheduled_sync_sent` → remove function + remove JobQueue registration
  - `_scheduled_pending_report` → use cache instead of `iter_rows_for_to_send`
  - `_scheduled_check_expired` → use new `expired_marker.run_check()`
  - `_scheduled_hunt` error text → remove `to_send.xlsx` mention
  - BotCommand list → remove `/sync_sent`, `/apply_expired`
  - `/start` help text → remove to_send mentions

## tools/

- [ ] `tools/backup_tracker.py` — remove to_send backup section
- [ ] `tools/fix_pracuj_urls.py` — remove to_send rewrite section
- [ ] `tools/repair_tracker.py` — remove `load_to_send_rows()` and its callers

## tests/

- [ ] `tests/test_tracker_backup.py` — remove to_send assertion
- [ ] `tests/test_tracker_service.py` — remove to_send monkeypatch setup

---

## Final verification (Phase 3.8)

```
grep -r "to_send" --include="*.py" hunter/ tests/ tools/ apply_agent.py generate_docs.py hunter.py
```
→ must return **zero matches**.
