# Что делать дальше — приоритезированный список

Горизонт: 2–6 недель. Каждый пункт ссылается на детальный путь (path-*.md),
где расписаны шаги. Порядок — по соотношению «ценность / усилие».

## P0 — быстрые победы (дни, почти без кода)

### 1. Настроить `LINKEDIN_STORAGE_STATE` в проде
Без Playwright-сессии **каждый** LinkedIn-fetch получает 429 — это главный
источник FAIL-строк в трекере. Один запуск `python tools/linkedin_login.py`
на деплой-хосте + переменная в `.env` + убрать `linkedin.com` из
`GMAIL_ENRICH_SKIP_HOSTS`. → [path-D-ops.md](path-D-ops.md), шаг D1.

### 2. Пересобрать prod-образ (E.1 из июньского ревью)
Inhire (Playwright) работает только на образе с текущим Dockerfile. Висит
с июня. → [path-D-ops.md](path-D-ops.md), шаг D2.

### 3. Две недели наблюдения за doomed-gate в проде
Gate только что уехал в master. Раз в несколько дней просматривать
SKIP-строки с его подписью: любой SKIP на вакансии, которую хотелось бы
подать — сигнал дёрнуть рычаг `DOOMED_GATE_HARD_ACTION=warn` и завести
регрессионный тест. → [path-B-cv-quality.md](path-B-cv-quality.md), шаг B1.

## P1 — превратить накопленные данные в решения (1–2 недели)

### 4. Вынести вердикт dual-apply: какая модель боевая?
Пары «primary vs shadow» с вердиктом одного судьи копятся с конца июня.
Посчитать по ним статистику + глазами сравнить 10–15 пар на Drive → решить:
остаёмся на Sonnet, переходим на DeepSeek (6× дешевле), или гибрид
(DeepSeek-генерация + Sonnet-refine). Дальше держать dual включённым «на
всякий случай» — только жечь деньги. → [path-A-feedback-loop.md](path-A-feedback-loop.md), шаг A2.

### 5. D.3: какие из 21 источников реально кормят воронку
`/funnel` уже считает tracked→generated→sent→confirmed→answered по источникам.
Снять срез за 60–90 дней, отключить мёртвый хвост (каждый источник — это
поддержка скрейпера). → [path-A-feedback-loop.md](path-A-feedback-loop.md), шаг A1.

### 6. Проверить корреляцию «ATS-вердикт ↔ ответы»
Вердикт пишется в колонку N с начала июля, ответы — в confirmation/answer.
Через месяц данных хватит на первый ответ: предсказывает ли цифра, ради
которой построен целый refine-цикл, хоть что-нибудь? Если нет — цель
`ATS_VERDICT_TARGET=95` пересмотреть (может, 85 достаточно, а это минус
раунды refine = минус деньги). → [path-A-feedback-loop.md](path-A-feedback-loop.md), шаг A3.

## P2 — дожать качество и закрыть отложенное (2–4 недели)

### 7. Precision-обзор judge → `JUDGE_MODE=block` (план M4)
Judge работает в `warn` с середины июня. Собрать judge_report.json за период,
посчитать точность fabrication-находок; если false positives ≈ 0 — включить
`block`. Заодно решить судьбу авто-репейра exaggeration.
→ [path-B-cv-quality.md](path-B-cv-quality.md), шаг B2.

### 8. Дедуп повторок («повторка») — отложенное решение владельца
Перепосты с новым URL и слегка изменённым названием проходят мимо
URL+company+title дедупа (задокументированы строки 674…762). Нужно решение:
нечёткий матчинг по company+нормализованный title с TTL? Пометка
Re-application вместо блока? → [path-E-expansion.md](path-E-expansion.md), шаг E1.

### 9. Распил `apply_shared.py` (A.1, отложен с июня)
Уже 1667 строк (в июне было 1320) — рост подтверждает диагноз «свалка».
PR #91 (claim_judge) давно смержен, блокер снят. Механический распил на
scrubs / review / notify. → [path-C-tech-debt.md](path-C-tech-debt.md), шаг C1.

## P3 — по остаточному принципу

- mypy-гейт хотя бы на новые модули (verdict_refine, dual_apply, funnel) —
  [path-C-tech-debt.md](path-C-tech-debt.md), шаг C2.
- Автоматизация еженедельного funnel-отчёта в Telegram —
  [path-A-feedback-loop.md](path-A-feedback-loop.md), шаг A4.
- Gmail-источник по умолчанию выключен (`GMAIL_ENABLED=false`) — включить,
  если LinkedIn-сессия (пункт 1) снимет главный источник 429 —
  [path-D-ops.md](path-D-ops.md), шаг D3.

## Чего по-прежнему НЕ делать (анти-цели, подтверждаю июньские)

- **Не добавлять источники ради объёма** — сначала пункт 5 покажет, сколько
  из текущих 21 вообще живут.
- **Не переписывать filters.py целиком** (1138 строк, некрасиво, но
  оттестировано и откалибровано) — только точечные фиксы на реальных
  false negatives/positives.
- **Не строить web-UI** — Telegram + Sheets покрывают single-user сценарий.
- **Не гнаться за новыми LLM-фичами**, пока пункты 4–6 не покажут, что
  текущие вообще окупаются.
