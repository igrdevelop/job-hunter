# Core
requests
cloudscraper
python-dotenv
openpyxl
python-docx

# Telegram bot
python-telegram-bot

# LLM providers (install at least one)
anthropic
# openai

# Optional: better HTML extraction for generic job pages
beautifulsoup4
pytest

# Optional: headless browser for JS-heavy / SPA job boards (inhire.io etc.)
# playwright  # install separately: pip install playwright && playwright install chromium
# playwright-stealth  # reduces bot-detection fingerprint for headless Playwright

google-api-python-client==2.131.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0