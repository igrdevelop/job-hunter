# Runtime dependencies — keep in sync with pyproject.toml [project.dependencies].
# Docker uses this file to install deps before `pip install -e . --no-deps`,
# so any new dependency must be added here AND in pyproject.toml.

# Core
requests
cloudscraper
python-dotenv
openpyxl
python-docx

# Telegram bot
python-telegram-bot[job-queue]
pytz

# LLM providers (install at least one)
anthropic
# openai

# HTML extraction
beautifulsoup4

# ATS scoring
scikit-learn

# Testing
pytest

# Optional: headless browser for JS-heavy / SPA job boards (inhire.io etc.)
playwright
# playwright-stealth  # reduces bot-detection fingerprint for headless Playwright

# Google APIs (pinned — breaking changes in minor versions)
google-api-python-client==2.131.0
google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
